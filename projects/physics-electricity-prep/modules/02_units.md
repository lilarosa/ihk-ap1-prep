# 2) 电学物理量与单位

## 定义 + 变量 + 单位
- 电荷 `Q`：单位库仑 `C`（1 C = 1 A·s）。【0†source】citeturn1search0turn2search0
- 电流 `I`：单位安培 `A`（1 A = 1 C/s）。【0†source】citeturn2search0turn2search4
- 电压 `U` 或 `V`：单位伏特 `V`（电势差单位）。【0†source】citeturn2search3
- 电阻 `R`：单位欧姆 `Ω`（1 Ω = 1 V/A）。【0†source】citeturn2search2
- 功率 `P`：单位瓦特 `W`（1 W = 1 J/s；P = V·I）。【0†source】citeturn4search0turn4search2
- 能量 `E`：单位焦耳 `J`（1 J = 1 W·s）。【0†source】citeturn3search0
- 频率 `f`：单位赫兹 `Hz`（1 Hz = 1/s）。【0†source】citeturn2search1

## 常用换算
- mA → A：除以 1000
- kΩ → Ω：乘以 1000
- kW → W：乘以 1000
- kWh → J：kWh × 3.6×10^6 J（1 Wh = 3600 J）【0†source】citeturn3search0turn4search2
